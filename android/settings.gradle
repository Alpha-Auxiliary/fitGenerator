pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// settings.gradle
dependencyResolutionManagement {
    repositories {
        // 优先使用国内镜像源
        maven { url 'https://maven.aliyun.com/repository/public/' } // 阿里云公共仓库[reference:18]
        maven { url 'https://maven.aliyun.com/repository/google' } // 阿里云 Google 镜像[reference:19]
        maven { url 'https://maven.aliyun.com/repository/jcenter' } // 阿里云 JCenter 镜像[reference:20]
        google()
        mavenCentral()
    }
}

rootProject.name = "FitGenerator"
include ":app"
