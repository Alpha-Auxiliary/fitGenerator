# 隐私说明

更新日期：2026-08-11

本说明适用于本仓库的原生 Windows 与 Android 客户端。它描述当前源码中的数据流，不构成法律意见，也不承诺第三方服务、操作系统或修改版应用的行为。

## 本地生成与本地数据

活动预览和 FIT 文件由设备上的 Rust 核心生成。项目没有用于上传路线、活动或设置的自有服务器，也没有在客户端代码中加入项目自有的分析、广告或用户账户系统。

用户绘制的 WGS84 路线、配速、心率、圈数、地图设置和预览种子可能保存在设备上。即使路线是模拟路线，它仍可能反映真实地点，请按位置数据保护。

- Windows 将设置和最近路线保存在当前用户的 `%LOCALAPPDATA%\FitGenerator.Native`。通过专用 API Key 字段配置的值使用 Windows DPAPI 的 `CurrentUser` 范围加密后写入设置文件。
- Android 将设置保存在应用私有 DataStore，将最近路线保存在应用私有 files 目录。通过专用 API Key 字段配置的值以 Android Keystore 中的 AES-GCM 密钥加密后写入 DataStore；普通设置和路线文件不保存该字段的明文值。
- Android Manifest 当前设置 `allowBackup="true"`。应用私有数据是否进入系统或厂商备份、保存多久以及能否恢复，由设备、系统版本和用户备份设置决定。Keystore 密钥可能无法随备份恢复，届时旧密文会被视为不可用。

不要把凭据直接粘贴进地图源名称、URL、归属说明或 Key 名称等普通配置字段；这些字段按普通设置保存，可能是明文并可能进入系统备份。应使用专用 API Key 字段。这些本地加密机制用于降低静态文件直接暴露的风险，不能抵御已经控制用户账户、设备管理员/root 权限、进程内存或操作系统的攻击者。

## 地图与地点搜索网络请求

仅在用户显示在线地图、搜索地点或测试地图连接时，客户端直接连接所选第三方地图/搜索提供商；请求不经过项目自有服务器。内置默认源使用 OpenStreetMap 瓦片与 Nominatim 搜索，自定义源由用户配置。

提供商通常会收到 IP 地址、User-Agent、请求时间和所请求的瓦片坐标；瓦片坐标及搜索请求可能反映地图视野或感兴趣的位置。地点搜索还会发送用户输入的查询、语言/结果数量等请求参数。若提供商要求 API Key，Windows 可按 URL 占位符、查询参数或请求头方式发送，其地图瓦片客户端已禁用自动重定向；Android 地图渲染只支持 URL 占位符或查询参数，会明确拒绝 Header Key 配置而不静默回退到其他地图源。URL 中的 Key 可能被提供商或网络基础设施记录。

内置默认端点使用 HTTPS。Android 自定义地图/搜索配置要求 HTTPS，Manifest 也禁用明文流量；Windows 自定义地图/搜索配置当前允许 HTTP 或 HTTPS。如果 Windows 用户选择 HTTP，查询、位置线索以及按 URL/查询参数发送的 Key 可能以明文经过网络，因此应只配置可信的 HTTPS 服务。HTTPS 仍不改变第三方提供商可读取其服务请求的事实。使用前请阅读相应提供商的隐私政策、使用条款和日志保留规则，并为 API Key 设置最低必要权限及额度。

## 导出与分享

Windows 把 FIT 文件写入用户选择的目录。Android 通过系统文件选择器（SAF）写入用户选择的文档，或把临时副本放在应用缓存的 `exports` 子目录，再通过受限的 `content://` URI 授予用户选择的分享目标只读访问。启动时会尽力清理旧分享缓存，但已保存、已发送或被接收方复制的文件不会由应用自动删除。

导出文件可能包含路线、时间、配速、心率和圈信息。用户应在分享前确认接收方和目标位置。

## 权限与日志

Android 当前只声明 `INTERNET` 权限；没有声明定位、后台定位、读取/写入共享存储或“所有文件访问”权限。应用不读取设备 GPS 位置。FileProvider 只授权应用缓存中的 `exports/`，共享权限随显式分享 Intent 授予。

项目没有持久化遥测日志服务。源码不会有意记录完整路线或地图 API Key；Windows 非交互自检可能向标准错误输出稳定错误信息。操作系统、设备厂商、安全软件、崩溃收集器、第三方地图服务和用户自行修改的构建可能另行记录数据，不受本项目控制。

## 删除和问题反馈

关闭应用后，Windows 用户可删除 `%LOCALAPPDATA%\FitGenerator.Native` 来移除本地设置和最近路线；Android 用户可在系统设置中清除应用数据或卸载应用。另行导出的文件、分享接收方副本以及系统/厂商备份必须在对应位置单独处理。

普通隐私问题可提交 GitHub issue，但请勿附上真实活动路线、API Key、设备备份、私密日志或其他敏感数据。安全漏洞请按 [SECURITY.md](SECURITY.md) 私下报告。

截至更新日期，本仓库尚无经本发布计划构建并验证的原生产物、SPDX SBOM 或 Rust `Cargo.lock`；本说明是对当前源码的静态描述，仍需在 Windows、Android 设备和发布 CI 中核验。
