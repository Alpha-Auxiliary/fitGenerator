# 安全策略

## 私密报告漏洞

请优先使用 GitHub 的 [Private vulnerability reporting](https://github.com/Alpha-Auxiliary/fitGenerator/security/advisories/new) 提交安全报告。建议包括受影响平台和版本、产物 SHA-256、经过脱敏的复现步骤、预期影响，以及可行的缓解建议。

不要在公开 issue、讨论、截图或附件中发布 API Key、keystore、签名密钥、token、真实活动路线、个人位置或未经脱敏的日志。如果仓库暂未启用 GitHub 私密漏洞报告，可新建一条不含漏洞细节、复现步骤或附件的公开 issue，仅说明“私密报告入口不可用”并请求维护者启用；在私密入口可用前不要提交敏感内容。本项目没有另行公布的安全邮箱。

维护者会在可用时间内尽力确认、评估和修复有效报告，并在适合时协调披露。项目不承诺固定响应时间、修复时限、服务等级、漏洞赏金或赔付。

## 支持范围

截至 2026-08-11，本仓库没有经当前发布流程构建、签名并验证的原生正式版本，因此暂无正式受支持的二进制版本。首次正式发布后，安全修复将优先面向最新补丁版本；旧版本是否获得修复会在对应公告中明确。`main` 分支仅作开发参考，不等同于已发布或已验证的产品。

安全问题包括但不限于：原生/JNI 内存安全、任意代码执行、路径逃逸或不安全文件授权、地图 Key 或路线泄露、权限扩大、不安全网络处理，以及可复现构建或发布供应链被绕过。仅涉及绕过学校或平台规则的使用方式不属于安全支持；本项目也不保证生成文件会被任何第三方平台接受。

## 发布物、签名与校验和边界

- Android 公开发布物应通过项目持有的 Android 签名密钥签名，并由 `apksigner` 验证。签名证明 APK 与该密钥关联，不证明代码无漏洞。
- Windows 在未获批或未启用可信签名服务时必须使用 `fit-generator-windows-x64-UNSIGNED.exe` 文件名。只有 Authenticode 状态为 `Valid` 的文件才可使用已签名文件名。
- `fit-generator-SHA256SUMS.txt` 可检测下载字节是否与发布记录一致，但不能单独证明发布者身份，也不能保护已经被攻击者同时替换的产物和校验和。
- 发布 metadata 记录调用者显式提供的版本与源码提交、文件大小、SHA-256 和待验证的签名状态；未提供版本/提交时会诚实写入 `UNSPECIFIED`，不能把该值当作来源证明。SPDX SBOM 与第三方 notices 用于描述组件来源和许可；它们不证明组件安全，也不能替代签名验证。

首次正式发布后，请只从项目的 GitHub Release 页面取得产物与校验和，并核对发布说明中的签名状态。不要运行来源不明、文件名掩盖 `UNSIGNED` 状态或校验失败的文件。

## 密钥与运行环境边界

签名私钥、Android keystore、密码和服务 token 不进入仓库、构建缓存、日志或发布附件。地图 API Key 是用户本地配置，不是项目发布密钥。若怀疑密钥泄露，请先在对应提供商撤销或轮换密钥，再通过私密渠道报告最少必要信息。

本项目的本地加密、签名与校验和不能防御已经控制操作系统、用户账户、管理员/root 权限、CI 管理权限或第三方地图提供商的攻击者。自定义构建、第三方重新打包、系统备份和导出后的文件也超出官方发布物的完整性边界。

## 当前发布前置状态

仓库已纳入审阅用 Rust `Cargo.lock`，并已移除具有宽泛触发器和发布权限的遗留 Web workflow。源码提交不包含 DLL、SO、APK、EXE 或 SPDX SBOM；它们必须由固定工具链的 CI/受保护发布流程重新生成并校验。Windows unsigned 单文件产物与核心自检已在开发主机验证，但 Android 正式签名、Windows 可选 Authenticode、metadata/verifier、SBOM 和完整发布链仍必须由受保护环境完成，不能从本地构建状态推断正式发布已受支持。
